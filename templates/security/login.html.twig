{% extends 'base.html.twig' %}

{% block title %}Iniciar Sesión{% endblock %}

{% block body %}
<div class="account-pages"></div>
<div class="clearfix"></div>
<div class="wrapper-page section">
    <div class="text-center">
        <a href="{{ path('dashboard') }}" class="logo"><img src="{{ asset('build/images/logoweb_mini.png') }}" alt="" srcset="" align="center"></a>
        <h5 class="text-muted m-t-0 font-600">Fabrica Chaqueña de Anafes y Termotanques</h5>
    </div>
    <div class="m-t-40 card-box login">
        <div class="text-center">
            <h4 class="title text-uppercase font-bold m-b-0">Inicio de Sesi&oacuten</h4>
        </div>
        <div class="p-20">
            <form method="post" class="form-horizontal m-t-20">

                <div class="form-group">
                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                    <div class="col-xs-12">
                        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required
                               autofocus placeholder="Email"
                        >
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-xs-12">
                        <input type="password" name="password" id="inputPassword"
                               class="form-control" required placeholder="Contraseña"
                        >
                    </div>
                </div>
                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}"
                >

                <div class="form-group ">
                    <div class="col-xs-12">
                        <div class="checkbox checkbox-custom">
                            <input type="checkbox" name="_remember_me" id="checkbox-signup">
                            <label for="checkbox-signup">
                                Recordar mis datos de inicio
                            </label>
                        </div>

                    </div>
                </div>

                <div class="form-group text-center m-t-30">
                    <div class="col-xs-12">
                        <button class="btn btn-custom btn-bordred btn-block waves-effect waves-light" type="submit">
                            Ingresar
                        </button>
                    </div>
                </div>

                <div class="form-group m-t-30 m-b-0">
                    <div class="col-sm-12">
                        <a href="#" class="text-muted">
                            <i class="fa fa-lock m-r-5"></i>
                            Ha olvidado su Contraseña?
                        </a>
                    </div>
                </div>
            </form>

            <a href="{{ path('app_register') }}" class="btn btn-secondary btn-bordred btn-block waves-effect waves-light">
                Registrar Usuario
            </a>

        </div>
    </div>
</div>
    {% endblock %}
